## What changed

Describe the problem and the solution.

## Verification

- [ ] `npm run build` passes in `web/` when client code changed
- [ ] `cargo test` passes in `server/` when server code changed
- [ ] I tested the affected voice/chat/screen/deploy flow
- [ ] I did not commit secrets, logs, chat data, or build artifacts

## Screenshots / logs

Add screenshots for visible UI changes and sanitized logs for networking fixes.
